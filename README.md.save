# PyShell 🐚

A Unix-like shell implemented in Python, supporting:
- Built-in commands (`cd`, `pwd`, `echo`, `type`, `history`)
- Command history with `HISTFILE` support
- Pipes (`|`) and redirection (`>`, `>>`, `<`)
- Environment variable expansion
- Tab completion using GNU Readline

## Features
- Bash-like history behavior (`history -a`, `-r`, `-w`, `-c`)
- Persistent history across sessions
- Linux-only implementation


## How to Run
```bash
python3 main.py

